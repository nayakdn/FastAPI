# python --version
#pip install fastapi
# pip install uvicorn
#pip3 install fastapi
#pip3 install uvicorn
#uvicorn 15main:app --reload
# git init
#git add . (git add  file namee) for sending the code again 

# git commit -m " FastAPI "  for sending the file again 
# git branch -M main 
#git remote add origin __link___
#git push -u origin main  for sending the file again 
# http://127.0.0.1:8000/docs
# control + c to end the backend session 
# for making the changes then existing file run three codes which we have to add the file and add . only should be used 
